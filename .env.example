# Copy this file to `.env` in the same directory (repo root) and set your token.
#   cp .env.example .env
# `.env` is gitignored.
#
# Hugging Face read token (for google/timesfm-2.5-200m-pytorch and similar):
#   https://huggingface.co/settings/tokens
HF_TOKEN=
