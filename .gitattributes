# Example outputs (png/gif) stored as normal Git blobs so public forks can push
# without new Git LFS uploads (GitHub restricts LFS on public forks).
timesfm-forecasting/**/*.png !text !filter !merge !diff
timesfm-forecasting/**/*.gif !text !filter !merge !diff
